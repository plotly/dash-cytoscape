# Packages needed to run the tests.
# Switch into a virtual environment
# pip install -r requirements.txt

chromedriver-binary
dash>=1.8.0
ipdb
percy
selenium
flake8
pylint
pytest-dash>=2.1.1
colour==0.1.5
biopython
